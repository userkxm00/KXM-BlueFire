export default function FAQ(){const items=[['Will KXM guarantee more FPS?','No. KXM changes configuration and measures outcomes where possible. Rendered FPS depends on the game, emulator, display, drivers and hardware.'],['Does KXM modify game files?','No. The safe path does not modify Free Fire files, inject DLLs or bypass anti-cheat.'],['Will my original settings be lost?','KXM is recovery-first. A baseline is stored outside the portable app folder before persistent changes, and GAME READY keeps a separate session state.'],['Should I use 240 FPS?','240 FPS is treated as an optional configuration ceiling, not a promise that Free Fire will render at 240 FPS.'],['What happens on an HDD + 8 GB system?','KXM uses a conservative storage policy and keeps SysMain on Auto rather than blindly disabling it.'],['Is community sharing enabled automatically?','No. Community telemetry is opt-in. The intended payload uses coarse hardware classes and outcomes rather than names, files or account credentials.']];return <main className="page"><div className="shell prose"><div className="kicker mono">FAQ</div><h1>Questions, answered without optimizer folklore.</h1>{items.map(([q,a])=><section className="card" style={{padding:24,marginTop:16}} key={q}><h2 style={{fontSize:24,marginTop:0}}>{q}</h2><p>{a}</p></section>)}</div></main>}