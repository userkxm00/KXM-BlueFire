"use client";
import { FormEvent,useState } from "react";
import { useRouter } from "next/navigation";
import { supabaseBrowser } from "../../../lib/supabase-browser";
export default function Login(){const[r,setR]=useState("");const[e,setE]=useState("");const[p,setP]=useState("");const router=useRouter();async function submit(ev:FormEvent){ev.preventDefault();setR("");const s=supabaseBrowser();const{error}=await s.auth.signInWithPassword({email:e,password:p});if(error){setR(error.message);return}router.push("/admin")}return <main className="login"><form className="card login-card" onSubmit={submit}><div className="kicker">KXM // ADMIN</div><h1 style={{fontSize:38,letterSpacing:"-.05em"}}>Community command center.</h1><p className="muted">Sign in with the administrator account configured in Supabase Auth.</p><div className="field"><label>Email</label><input type="email" value={e} onChange={x=>setE(x.target.value)} required autoComplete="email"/></div><div className="field"><label>Password</label><input type="password" value={p} onChange={x=>setP(x.target.value)} required autoComplete="current-password"/></div>{r&&<p className="bad">{r}</p>}<button className="btn btn-primary" style={{width:"100%",marginTop:22}} type="submit">Sign in</button></form></main>}
