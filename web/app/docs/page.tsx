export default function Docs(){return <main className="page"><div className="shell prose"><div className="kicker">Documentation</div><h1>KXM BlueFire, explained.</h1><p>BlueFire is a Windows gaming performance toolkit built around a recovery-first workflow: detect the machine, protect its current state, recommend only compatible changes, measure the result and keep rollback obvious.</p><h2>1. Detect</h2><p>KXM reads CPU, threads, RAM, GPU, storage class, virtualization and emulator context. Disk detection has a fallback path so older systems can still be classified as HDD or SSD.</p><h2>2. Protect</h2><p>Before a persistent configuration change, KXM stores a baseline outside the portable application directory under <span className="mono">C:\ProgramData\KXM\BlueFire\Backups</span>. A session snapshot is separate from the persistent baseline.</p><h2>3. Recommend</h2><p>Free Fire / BlueStacks starts from a 4-core / 4-GB baseline when the machine can support it. Smaller systems scale down. HDD or 8-GB-and-under systems keep SysMain automatic.</p><h2>4. Game Ready</h2><p>The session mode cleans safe temporary locations, flushes DNS, switches to the KXM gaming power profile and can raise the BlueStacks player process priority while the session is active.</p><h2>5. Measure</h2><p>KXM keeps measurement provenance explicit. Average FPS claims are not invented; frame-time tooling can use PresentMon when installed, and benchmark snapshots can be saved for before/after comparison.</p><h2>6. Learn</h2><p>Community sharing is opt-in and coarse. The online pipeline is designed to aggregate outcomes by hardware class, game and profile rather than storing personal information or raw Windows configuration dumps.</p><h2>7. Restore</h2><p><span className="mono">END SESSION</span> restores the session power state. <span className="mono">RESTORE ORIGINAL</span> restores the saved persistent baseline. This is configuration recovery, not a full disk image.</p><h2>Safety boundary</h2><p>The safe path does not intentionally remove Defender, disable Windows Update, disable the pagefile, force HPET, blindly force MSI mode, remove Edge/WebView2, modify game files, inject DLLs, bypass anti-cheat or mass-disable Windows services.</p><div className="code mono">Detect → Protect → Recommend → Preview → Apply → Measure → Learn → Restore</div></div></main>}
