import { Cpu, GameController, HardDrive, Lightning, Memory, ShieldCheck } from "@phosphor-icons/react/dist/ssr";

const profiles = [
  {name:"Free Fire / Low-End",icon:GameController,desc:"HDD + 8GB class machines. Conservative settings with stability first.",items:[["BlueStacks CPU","4 cores when supported"],["Memory","4 GB when supported"],["Power","High Performance"],["SysMain","Keep Auto on HDD / <=8GB"]]},
  {name:"Free Fire / Competitive",icon:Lightning,desc:"More aggressive session tuning for systems with comfortable headroom.",items:[["BlueStacks CPU","4–6 cores based on CPU"],["Memory","4–8 GB based on RAM"],["Power","KXM gaming plan"],["Measurement","Frame-time preferred"]]},
  {name:"Safe Windows",icon:ShieldCheck,desc:"Minimal-change profile for people who want only reversible baseline improvements.",items:[["Services","No mass purge"],["Pagefile","Keep managed"],["Security","Defender untouched"],["Recovery","Baseline before persistent changes"]]}
];
export default function Profiles(){return <main className="page"><div className="shell"><div className="kicker">Profiles</div><h1 className="section-title" style={{maxWidth:900}}>The profile follows the machine. Not the other way around.</h1><p className="section-copy muted">KXM uses hardware classes, storage type, RAM and emulator context to choose a starting point. Smaller systems scale down instead of being forced into a desktop preset.</p><div className="profiles" style={{marginTop:48}}>{profiles.map((p)=>{const I=p.icon;return <article className="card profile" key={p.name}><I size={30} color="var(--accent)"/><h3>{p.name}</h3><p className="muted" style={{lineHeight:1.7}}>{p.desc}</p><ul>{p.items.map(([a,b])=><li key={a}><span>{a}</span><strong>{b}</strong></li>)}</ul></article>})}</div><div className="card" style={{padding:28,marginTop:24,display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:18}}><div><Cpu size={24} color="var(--accent)"/><h3>CPU-aware</h3><p className="muted">Thread count affects the BlueStacks recommendation.</p></div><div><Memory size={24} color="var(--accent)"/><h3>RAM-aware</h3><p className="muted">KXM avoids starving Windows to feed the emulator.</p></div><div><HardDrive size={24} color="var(--accent)"/><h3>Storage-aware</h3><p className="muted">HDD systems keep SysMain and avoid aggressive cache destruction.</p></div></div></div></main>}
